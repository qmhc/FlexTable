export const temp = document.createElement('div');

export const buttonTemp = document.createElement('button');
buttonTemp.className = 'it-button';

export const spanTemp = document.createElement('span');

export const inputTemp = document.createElement('input');
inputTemp.className = 'it-input';

export const tableTemp = temp.cloneNode();
tableTemp.className = 'it-table';

export const theadTemp = temp.cloneNode();
theadTemp.className = 'it-thead';

export const tbodyTemp = temp.cloneNode();
tbodyTemp.className = 'it-tbody';

export const thTemp = temp.cloneNode();
thTemp.className = 'it-th';

// 用于行内拓展
export const trGroupTemp = temp.cloneNode();
trGroupTemp.className = 'it-tr-group';

export const trTemp = temp.cloneNode();
trTemp.className = 'it-tr';

export const tdTemp = temp.cloneNode();
tdTemp.className = 'it-td';